# 开发调整记录

## 调整日期：2026-05-10

### 1. 前端技术栈废弃（Vue 方案）
**原因**：原计划使用 Vue 3 + Aceternity UI，但 Aceternity UI 官方仅支持 React不支持 Vue。

**操作**：删除 `code/frontend/` 目录下所有内容，标记 Vue 方案废弃。

### 2. 前端技术栈变更（Vue → React）
**新方案**：React 18 + TypeScript + Vite + pnpm

**技术选型**：
| 类别 | 技术 |
|------|------|
| 框架 | React 18 + TypeScript |
| 构建 | Vite |
| UI | Aceternity UI |
| 路由 | React Router 6 |
| 状态 | Zustand |
| 图标 | Lucide React |

**变更位置**：`code/frontend/`

---

## 调整日期：2026-05-09

## 一、开发计划调整

### 1. 单元测试阶段跳过
**原因**：项目初期无测试基础设施，单元测试需要配置 H2 内存数据库和 MockMvc，环境复杂。为了加快开发进度，单元测试暂时跳过。

**后续**：可在项目稳定后补充单元测试。

### 2. Java 版本调整
**原始计划**：Java 21
**实际情况**：系统环境为 Java 17
**调整**：将 pom.xml 中的 java.version 从 21 改为 17

### 3. MyBatis-Plus 版本调整
**原始计划**：4.0.1
**实际情况**：阿里云 Maven 仓库无可用版本
**调整**：
- `mybatis-plus-spring-boot-starter` → `mybatis-plus-boot-starter`，版本 3.5.3.1

### 4. EasyExcel 依赖调整
**原始计划**：`com.github.whvcse:easyexcel:3.3.4`
**实际情况**：该包在阿里云仓库不可用
**调整**：`com.alibaba:easyexcel:3.3.4`

---

## 二、修复的问题

### 1. 实体类缺少 Lombok @Data 注解
**问题**：实体类继承 BaseEntity 后，没有生成 getter/setter 方法
**解决**：为所有实体类添加 `@Data` 注解

### 2. AuthInterceptor 中 ResultCode 类型错误
**问题**：sendUnauthorized 方法参数类型不匹配
**解决**：将 `ResultCode code` 改为 `int code`

### 3. Result.fail(ResultCode) 方法调用不存在的方法
**问题**：ResultCode 是常量类，不是枚举，没有 getCode() 和 name() 方法
**解决**：删除该重载方法，仅使用 `fail(int code, String message)`

### 4. OperatorService.confirmOrder 调用不存在的方法
**问题**：调用了 `changeOrderStatus` 方法，但该方法不存在
**解决**：将逻辑内联到 confirmOrder 方法中

### 5. UserService 中 Integer/Long 类型不匹配
**问题**：`orderMapper.selectCount()` 返回 Long，而非 Integer
**解决**：将变量类型从 `Integer` 改为 `Long`

### 6. SnowflakeIdUtil 导入了不存在的类
**问题**：导入了 `com.github.qcloudsms.SmsSingleSender`
**解决**：删除无用导入

### 7. 缺少必要依赖
**问题**：缺少 validation、security-crypto、jackson-databind
**解决**：在 pom.xml 中补充这些依赖

---

## 三、项目结构确认

### 后端结构
```
code/backend/ai-shop/src/main/java/com/sh/aishop/
├── AiShopApplication.java
├── config/
│   ├── WebMvcConfig.java
│   ├── RedisConfig.java
│   └── MyMetaObjectHandler.java
├── interceptor/
│   └── AuthInterceptor.java
├── entity/
│   ├── BaseEntity.java
│   ├── User.java, Shop.java, Category.java, Product.java
│   ├── Order.java, Points.java, Address.java, InviteCode.java, Message.java
│   └── enums/ (9个枚举)
├── dto/
│   ├── UserDTO.java, ShopDTO.java, OrderDTO.java, ProductDTO.java
│   ├── PageRequest.java, PageResult.java
│   ├── LoginRequest.java, RegisterRequest.java, PasswordRequest.java
├── mapper/ (9个 Mapper)
├── service/
│   ├── AuthService.java
│   ├── AdminService.java
│   ├── OperatorService.java
│   └── UserService.java
├── controller/
│   ├── AuthController.java
│   ├── AdminController.java
│   ├── OperatorController.java
│   └── UserController.java
├── common/
│   ├── Result.java
│   └── ResultCode.java
└── util/
    ├── JwtUtil.java
    ├── SecurityUtil.java
    ├── SnowflakeIdUtil.java
    └── BeanUtil.java
```

### 前端结构
```
code/frontend/src/
├── main.ts
├── App.vue
├── style.css
├── router/index.ts
├── stores/ (auth.ts, app.ts)
├── api/ (index.ts, auth.ts, admin.ts, operator.ts, user.ts)
├── views/
│   ├── Login.vue, Register.vue
│   ├── layout/Layout.vue
│   ├── admin/ (ShopManage.vue, UserManage.vue)
│   ├── operator/ (MyShop.vue, CategoryManage.vue, ProductManage.vue, OrderManage.vue, CustomerManage.vue, Messages.vue)
│   └── user/ (ProductList.vue, ProductDetail.vue, MyOrders.vue, AddressBook.vue, MyMessages.vue)
├── components/ (Header.vue, ProductCard.vue, OrderCard.vue)
└── types/index.ts
```

---

## 四、构建结果

### 后端 JAR 包
- 路径：`code/backend/ai-shop/target/ai-shop-0.0.1-SNAPSHOT.jar`
- 状态：✅ 构建成功

### 前端
- 状态：✅ 已配置完成，`pnpm dev` 可正常启动

---

## 五、待完成事项

1. **数据库初始化**：需在 MySQL 中执行 `code/backend/sql/init.sql` 创建数据库和表
2. **Redis 启动**：确保 Redis 服务运行在 localhost:6379
3. **前后端联调**：前端代理已配置，后端启动后即可联调

---

## 四、2026-05-09 第二轮调整

### 1. Java 版本调整（回退）
**原始调整**：Java 17
**实际情况**：用户已将 Java 环境升级到 JDK 21
**调整**：将 pom.xml 中的 java.version 从 17 改回 21

### 2. 前端玻璃拟态重构
**背景**：所有前端页面无统一样式，使用基础 Tailwind 类
**操作**：
- 创建全局玻璃拟态（Glassmorphism）设计系统（style.css），包含 CSS 变量、玻璃面板、输入框、按钮、表格、分页、模态框等全套组件类
- 更新 index.html（标题、Google Fonts 引入）
- 更新 App.vue（添加路由过渡动画）
- 重构 Login.vue、Register.vue → 玻璃卡牌风格
- 重构 Layout.vue 侧边栏 → 玻璃毛玻璃效果
- 重构 Header.vue 导航栏 → 玻璃风格
- 重构 Admin 页面（ShopManage.vue、UserManage.vue）→ 玻璃表格
- 重构 Operator 页面（MyShop、CategoryManage、ProductManage、OrderManage、CustomerManage、Messages）→ 玻璃表格/卡片
- 重构 User 页面（ProductList、ProductDetail、MyOrders、AddressBook、MyMessages）→ 玻璃卡片
- 重构通用组件（ProductCard.vue、OrderCard.vue）→ 玻璃卡片

**结果**：前端 21 个 Vue 文件全部重构为一致的玻璃拟态风格

### 3. 后端单元测试完成
**原始**：单元测试跳过
**实际情况**：已完成全部后端 Service 层单元测试
**操作**：
- AuthServiceTest.java：5 个测试分组（login/register/getUserInfo/changePassword/logout），共 16 个测试用例
- AdminServiceTest.java：7 个测试分组（getShops/auditShop/getUsers/changeUserStatus/approveUser/getInviteCode/createInviteCode），共 21 个测试用例  
- OperatorServiceTest.java：涵盖店铺/分类/商品/订单/客户/邀请码/消息管理，共 57 个测试用例
- UserServiceTest.java：涵盖商品/订单/积分/地址/消息管理，共 35 个测试用例

**构建结果**：
- `mvnw test`：✅ **Tests run: 129, Failures: 0, Errors: 0, Skipped: 0**
- 使用 Mockito + JUnit 5 纯单元测试，不依赖数据库环境
- 前端 LSP 诊断：0 errors

### 4. Mockito Agent 警告修复
**问题**：JDK 21 下 Mockito 动态加载 agent 产生警告
```
Mockito is currently self-attaching to enable the inline-mock-maker...
Dynamic loading of agents will be disallowed by default in a future release
```
**解决**：在 pom.xml maven-surefire-plugin 中添加 `-XX:+EnableDynamicAgentLoading` JVM 参数
**结果**：警告消除，测试正常通过

---

## 文档版本：v2.0
**创建日期**：2026-05-09
**最后更新**：2026-05-09