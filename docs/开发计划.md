# 积分商城系统 - 后端开发计划

## 一、环境信息

| 项目 | 配置 |
|------|------|
| JDK | 21，`C:\Program Files\Java\jdk21` |
| Maven | 3.9.9，`C:\server\maven\apache-maven-3.9.9` |
| MySQL | `root/root`，localhost:3306，数据库 `ai_shop` |
| Redis | localhost:6379，无密码 |
| 前端端口 | 18781（Vite 开发服务器，对前端暴露 18780 通过 proxy 转发） |
| 后端端口 | 18780（Spring Boot） |

---

## 二、任务总览

| 阶段 | 任务数 | 预计优先级 |
|------|--------|-----------|
| 阶段一：基础设施 | 3 | high |
| 阶段二：实体+枚举+工具 | 4 | high |
| 阶段三：认证模块 | 6 | high |
| 阶段四：管理员接口 | 6 | high |
| 阶段五：店铺用户接口 | 17 | high |
| 阶段六：普通用户接口 | 14 | high |
| 阶段七：单元测试 | 4 | high |
| 阶段八：构建验证 | 1 | high |

---

## 三、阶段详情

### 阶段一：基础设施

| 序号 | 任务 | 产出文件 |
|------|------|----------|
| 1.1 | 创建 MySQL 数据库脚本 | `code/backend/sql/init.sql` |
| 1.2 | 配置 application.properties | `src/main/resources/application.properties` |
| 1.3 | 补充 Maven 依赖 | `pom.xml` |

#### 1.1 MySQL 建表 SQL（9张表）

```sql
-- 数据库：ai_shop
CREATE DATABASE IF NOT EXISTS ai_shop DEFAULT CHARSET utf8mb4;

USE ai_shop;

-- 1. user 用户表
CREATE TABLE user (
  id BIGINT PRIMARY KEY COMMENT '雪花ID',
  username VARCHAR(50) NOT NULL COMMENT '用户名',
  nickname VARCHAR(50) COMMENT '昵称',
  password VARCHAR(255) NOT NULL COMMENT '密码BCrypt加密',
  role TINYINT NOT NULL COMMENT '1=管理员 2=店铺用户 3=普通用户',
  parent_id BIGINT DEFAULT 0 COMMENT '上级用户ID',
  status TINYINT DEFAULT 1 COMMENT '1=待审核 2=正常 3=已冻结',
  deleted TINYINT DEFAULT 0 COMMENT '0=未删除 1=已删除',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='用户表';

-- 2. shop 店铺表
CREATE TABLE shop (
  id BIGINT PRIMARY KEY,
  operator_id BIGINT NOT NULL COMMENT '店铺用户ID',
  name VARCHAR(100) NOT NULL COMMENT '店铺名称',
  description TEXT COMMENT '店铺简介',
  status TINYINT DEFAULT 1 COMMENT '1=待审核 2=已通过 3=已拒绝 4=已禁用',
  is_active TINYINT DEFAULT 1 COMMENT '0=歇业 1=营业',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='店铺表';

-- 3. category 商品分类表
CREATE TABLE category (
  id BIGINT PRIMARY KEY,
  shop_id BIGINT NOT NULL COMMENT '店铺ID',
  name VARCHAR(50) NOT NULL COMMENT '分类名称',
  sort INT DEFAULT 0 COMMENT '排序',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='商品分类表';

-- 4. product 商品表
CREATE TABLE product (
  id BIGINT PRIMARY KEY,
  shop_id BIGINT NOT NULL COMMENT '店铺ID',
  category_id BIGINT COMMENT '分类ID',
  name VARCHAR(100) NOT NULL COMMENT '商品名称',
  type TINYINT NOT NULL COMMENT '1=虚拟 2=实体',
  price INT NOT NULL COMMENT '积分价格',
  stock INT DEFAULT -1 COMMENT '-1=无限 0=不可下单',
  limit_per_user INT DEFAULT 0 COMMENT '0=不限',
  main_image VARCHAR(255) COMMENT '主图URL',
  detail_images TEXT COMMENT '详情图JSON数组',
  description TEXT COMMENT '商品描述',
  delivery_info TEXT COMMENT '发货说明',
  status TINYINT DEFAULT 1 COMMENT '1=上架 2=下架',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='商品表';

-- 5. orders 订单表（含收货地址快照）
CREATE TABLE orders (
  id BIGINT PRIMARY KEY,
  order_no VARCHAR(32) NOT NULL UNIQUE COMMENT '订单号唯一',
  user_id BIGINT NOT NULL COMMENT '普通用户ID',
  shop_id BIGINT NOT NULL COMMENT '店铺ID',
  product_id BIGINT NOT NULL COMMENT '商品ID',
  points INT NOT NULL COMMENT '消耗积分',
  quantity INT DEFAULT 1 COMMENT '兑换数量',
  status TINYINT DEFAULT 1 COMMENT '1=已下单 2=已确认 3=已发货 4=已完成 5=已关闭',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  completed_at DATETIME COMMENT '完成时间',
  closed_at DATETIME COMMENT '关闭时间',
  close_reason VARCHAR(255) COMMENT '关闭原因',
  receiver_name VARCHAR(50) COMMENT '收货人姓名',
  receiver_phone VARCHAR(20) COMMENT '手机号',
  receiver_province VARCHAR(50) COMMENT '省',
  receiver_city VARCHAR(50) COMMENT '市',
  receiver_district VARCHAR(50) COMMENT '区',
  receiver_detail VARCHAR(255) COMMENT '详细地址',
  express_company VARCHAR(50) COMMENT '快递公司',
  express_no VARCHAR(100) COMMENT '快递单号',
  delivery_content TEXT COMMENT '发货内容（虚拟商品）'
) COMMENT='订单表';

-- 6. points 积分表
CREATE TABLE points (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '用户ID',
  amount INT NOT NULL COMMENT '积分变动（正=发放 负=扣除）',
  balance INT NOT NULL COMMENT '变动后余额',
  type TINYINT NOT NULL COMMENT '1=发放 2=扣除 3=兑换 4=退款',
  remark VARCHAR(255) COMMENT '备注',
  operator_id BIGINT COMMENT '操作人ID',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='积分表';

-- 7. address 收货地址表
CREATE TABLE address (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '用户ID',
  name VARCHAR(50) NOT NULL COMMENT '收货人姓名',
  phone VARCHAR(20) NOT NULL COMMENT '手机号',
  province VARCHAR(50) NOT NULL COMMENT '省',
  city VARCHAR(50) NOT NULL COMMENT '市',
  district VARCHAR(50) NOT NULL COMMENT '区',
  detail VARCHAR(255) NOT NULL COMMENT '详细地址',
  is_default TINYINT DEFAULT 0 COMMENT '0=否 1=是',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='收货地址表';

-- 8. invite_code 邀请码表
CREATE TABLE invite_code (
  id BIGINT PRIMARY KEY,
  code VARCHAR(20) NOT NULL UNIQUE COMMENT '邀请码',
  role TINYINT NOT NULL COMMENT '2=店铺用户 3=普通用户',
  creator_id BIGINT NOT NULL COMMENT '创建者ID',
  status TINYINT DEFAULT 1 COMMENT '1=有效 2=已作废',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='邀请码表';

-- 9. message 消息表
CREATE TABLE message (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '接收用户ID',
  title VARCHAR(100) NOT NULL COMMENT '消息标题',
  content TEXT COMMENT '消息内容',
  type TINYINT NOT NULL COMMENT '1=积分通知 2=订单通知',
  related_id BIGINT COMMENT '关联ID（订单ID或积分ID）',
  is_read TINYINT DEFAULT 0 COMMENT '0=未读 1=已读',
  deleted TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='消息表';

-- 预置管理员账号（用户名：admin，密码：admin123）
-- 密码为 BCrypt 加密后的值
INSERT INTO user (id, username, nickname, password, role, parent_id, status, deleted)
VALUES (1, 'admin', '管理员', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsjM/tUo7l2sKuvIlTSfG', 1, 0, 2, 0);
```

#### 1.2 application.properties

```properties
spring.application.name=ai-shop

# 数据库
spring.datasource.url=jdbc:mysql://localhost:3306/ai_shop?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0

# JWT
jwt.secret=ai-shop-jwt-secret-key-2026-very-long-string-for-security
jwt.expiration=604800000  # 7天（毫秒）

# MyBatis-Plus
mybatis-plus.mapper-locations=classpath:mapper/**/*.xml
mybatis-plus.type-aliases-package=com.sh.aishop.entity
mybatis-plus.configuration.map-underscore-to-camel-case=true
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# 服务器
server.port=18780

# 前端代理配置（vite.config.ts）
# /api -> http://localhost:18780
```

#### 1.3 Maven 依赖（pom.xml 追加）

```xml
<!-- MyBatis-Plus -->
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-spring-boot-starter</artifactId>
    <version>4.0.1</version>
</dependency>

<!-- Redis Session -->
<dependency>
    <groupId>org.springframework.session</groupId>
    <artifactId>spring-session-data-redis</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>

<!-- JWT -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.5</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.5</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.5</version>
    <scope>runtime</scope>
</dependency>

<!-- 雪花ID -->
<dependency>
    <groupId>com.github.whvcse</groupId>
    <artifactId>easyexcel</artifactId>
    <version>3.3.4</version>
</dependency>
```

---

### 阶段二：实体 + 枚举 + 工具

| 序号 | 任务 | 产出文件 |
|------|------|----------|
| 2.1 | 枚举类（9个） | `entity/enums/` |
| 2.2 | 实体类（9个） | `entity/` |
| 2.3 | 通用响应类 | `common/Result.java`、`common/ResultCode.java` |
| 2.4 | 工具类 | `util/JwtUtil.java`、`util/SecurityUtil.java`、`util/SnowflakeIdUtil.java` |

#### 2.1 枚举类

| 文件名 | 枚举值 |
|--------|--------|
| `RoleEnum.java` | ADMIN(1,"管理员"), SHOP_USER(2,"店铺用户"), NORMAL_USER(3,"普通用户") |
| `UserStatus.java` | PENDING(1,"待审核"), NORMAL(2,"正常"), FROZEN(3,"已冻结") |
| `ShopStatus.java` | PENDING(1,"待审核"), APPROVED(2,"已通过"), REJECTED(3,"已拒绝"), DISABLED(4,"已禁用") |
| `ProductType.java` | VIRTUAL(1,"虚拟"), PHYSICAL(2,"实体") |
| `ProductStatus.java` | ON_SALE(1,"上架"), OFF_SALE(2,"下架") |
| `OrderStatus.java` | CREATED(1,"已下单"), CONFIRMED(2,"已确认"), SHIPPED(3,"已发货"), COMPLETED(4,"已完成"), CLOSED(5,"已关闭") |
| `PointsType.java` | GRANT(1,"发放"), DEDUCT(2,"扣除"), EXCHANGE(3,"兑换"), REFUND(4,"退款") |
| `InviteCodeStatus.java` | ACTIVE(1,"有效"), INVALID(2,"已作废") |
| `MessageType.java` | POINTS(1,"积分通知"), ORDER(2,"订单通知") |

#### 2.2 实体类（继承 BaseEntity 实现自动填充）

所有实体继承 `com.baomidou.mybatisplus.extension.activerecord.Model`，配置：
- 主键类型：`@TableId(type = IdType.ASSIGN_ID)`
- 逻辑删除：`@TableLogic`
- 自动填充：创建时间 `created_at`、更新时间 `updated_at` 通过 MetaObjectHandler 实现

---

### 阶段三：认证模块

| 序号 | 任务 | 说明 |
|------|------|------|
| 3.1 | JwtUtil | 生成/解析 Token，Redis 存储 Token 黑名单 |
| 3.2 | AuthInterceptor | 验证 JWT Header，拦截需要认证的请求 |
| 3.3 | AuthController | `/api/auth/*` 四个接口 |
| 3.4 | AuthService | 认证业务逻辑 |

#### 3.3 认证接口设计

| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `/api/auth/login` | POST | `{username, password}` | `{token, userinfo}` |
| `/api/auth/register` | POST | `{username, nickname, password, confirmPassword, inviteCode}` | `{userinfo}` |
| `/api/auth/password` | PUT | `{oldPassword, newPassword}` (需认证) | `{success}` |
| `/api/auth/logout` | POST | Header: Authorization | `{success}` |
| `/api/auth/info` | GET | Header: Authorization | `{userinfo, points}` |

---

### 阶段四：管理员接口

| 序号 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 4.1 | `/api/admin/shops` | GET | 店铺列表（分页） |
| 4.2 | `/api/admin/shops/{id}/audit` | PUT | 审核店铺 `{status:2=通过,3=拒绝}` |
| 4.3 | `/api/admin/users` | GET | 用户列表（店铺用户+普通用户） |
| 4.4 | `/api/admin/users/{id}/status` | PUT | 冻结/解冻 `{status:3=冻结,2=解冻}` |
| 4.5 | `/api/admin/users/{id}/approve` | PUT | 审核通过店铺用户 |
| 4.6 | `/api/admin/invite-code` | GET/POST | 获取/生成邀请码 |

---

### 阶段五：店铺用户接口

| 序号 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 5.1 | `/api/operator/shop` | GET | 获取我的店铺 |
| 5.2 | `/api/operator/shop` | POST | 申请店铺 `{name, description}` |
| 5.3 | `/api/operator/shop/status` | PUT | 切换营业/歇业 `{isActive:0/1}` |
| 5.4 | `/api/operator/categories` | GET | 分类列表 |
| 5.5 | `/api/operator/categories` | POST | 创建分类 `{name, sort}` |
| 5.6 | `/api/operator/categories/{id}` | PUT | 编辑分类 `{name, sort}` |
| 5.7 | `/api/operator/categories/{id}` | DELETE | 删除分类 |
| 5.8 | `/api/operator/products` | GET | 商品列表（分页+搜索） |
| 5.9 | `/api/operator/products` | POST | 创建商品（多字段） |
| 5.10 | `/api/operator/products/{id}` | GET/PUT/DELETE | 商品详情/编辑/删除 |
| 5.11 | `/api/operator/orders` | GET | 订单列表（分页+状态筛选） |
| 5.12 | `/api/operator/orders/{id}/confirm` | PUT | 确认订单 |
| 5.13 | `/api/operator/orders/{id}/ship` | PUT | 发货（虚拟填 deliveryContent，实体填快递信息） |
| 5.14 | `/api/operator/orders/{id}/close` | PUT | 关闭订单（退回积分） |
| 5.15 | `/api/operator/orders/{id}/complete` | PUT | 完成订单 |
| 5.16 | `/api/operator/users` | GET | 普通用户列表 |
| 5.17 | `/api/operator/users/{id}/points` | POST | 发放/扣除积分 `{amount, remark}` |
| 5.18 | `/api/operator/users/{id}/points/log` | GET | 积分流水 |
| 5.19 | `/api/operator/users/{id}/approve` | PUT | 审核通过普通用户 |
| 5.20 | `/api/operator/invite-code` | GET/POST | 获取/生成邀请码 |
| 5.21 | `/api/operator/users/create` | POST | 创建普通用户（无需邀请码） |
| 5.22 | `/api/operator/users/import` | POST | Excel导入用户 |
| 5.23 | `/api/operator/users/{id}/reset-password` | PUT | 重置密码 |
| 5.24 | `/api/operator/messages` | GET | 消息列表 |
| 5.25 | `/api/operator/messages/{id}/read` | PUT | 标记已读 |

---

### 阶段六：普通用户接口

| 序号 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 6.1 | `/api/user/products` | GET | 商品列表（只看营业中店铺，分页） |
| 6.2 | `/api/user/products/{id}` | GET | 商品详情 |
| 6.3 | `/api/user/orders` | POST | 下单 `{productId, quantity, addressId}` |
| 6.4 | `/api/user/orders` | GET | 订单列表 |
| 6.5 | `/api/user/orders/{id}` | GET | 订单详情 |
| 6.6 | `/api/user/orders/{id}/close` | PUT | 关闭订单（退回积分） |
| 6.7 | `/api/user/orders/{id}/complete` | PUT | 完成订单 |
| 6.8 | `/api/user/points` | GET | 积分余额 |
| 6.9 | `/api/user/points/log` | GET | 积分流水 |
| 6.10 | `/api/user/addresses` | GET/POST | 地址列表/新增 |
| 6.11 | `/api/user/addresses/{id}` | PUT/DELETE | 编辑/删除地址 |
| 6.12 | `/api/user/addresses/{id}/default` | PUT | 设置默认地址 |
| 6.13 | `/api/user/messages` | GET | 消息列表 |
| 6.14 | `/api/user/messages/{id}/read` | PUT | 标记已读 |

---

### 阶段七：单元测试

使用 `@SpringBootTest` + `MockMvc`，每个 Controller 一个测试类：

| 测试类 | 测试范围 |
|--------|----------|
| `AuthControllerTest.java` | 登录（成功/失败）、注册（成功/邀请码无效/重复用户）、Token 解析、修改密码 |
| `AdminControllerTest.java` | 店铺审批（通过/拒绝）、用户冻结/解冻、approve 审核 |
| `OperatorControllerTest.java` | 商品 CRUD、订单状态流转（确认→发货→完成）、积分发放/扣除 |
| `UserControllerTest.java` | 下单扣积分、地址管理、积分流水查询 |

**测试要求：**
- 所有接口必须有单元测试
- 使用 H2 内存数据库进行测试（`@ActiveProfiles("test")`）
- 每个测试方法相互独立，不依赖顺序
- 断言覆盖：HTTP 状态码 + 响应体 code + 关键数据

---

### 阶段八：构建验证

```bash
cd code/backend/ai-shop
mvn clean package -DskipTests=false
```

预期产出：`target/ai-shop-0.0.1-SNAPSHOT.jar`

---

## 四、项目结构（最终）

```
code/backend/ai-shop/src/main/java/com/sh/aishop/
├── AiShopApplication.java
├── config/
│   ├── WebMvcConfig.java          # 拦截器 + 跨域配置
│   └── RedisConfig.java
├── interceptor/
│   └── AuthInterceptor.java       # JWT 认证拦截器
├── entity/
│   ├── enums/
│   │   ├── RoleEnum.java
│   │   ├── UserStatus.java
│   │   ├── ShopStatus.java
│   │   ├── ProductType.java
│   │   ├── ProductStatus.java
│   │   ├── OrderStatus.java
│   │   ├── PointsType.java
│   │   ├── InviteCodeStatus.java
│   │   └── MessageType.java
│   ├── User.java
│   ├── Shop.java
│   ├── Category.java
│   ├── Product.java
│   ├── Order.java
│   ├── Points.java
│   ├── Address.java
│   ├── InviteCode.java
│   └── Message.java
├── common/
│   ├── Result.java                # 统一响应体
│   └── ResultCode.java            # 响应码定义
├── util/
│   ├── JwtUtil.java               # JWT 工具类
│   ├── SecurityUtil.java          # BCrypt 工具类
│   ├── SnowflakeIdUtil.java       # 雪花 ID 生成器
│   └── BeanUtil.java              # Bean 拷贝工具
├── mapper/
│   ├── UserMapper.java
│   ├── ShopMapper.java
│   ├── CategoryMapper.java
│   ├── ProductMapper.java
│   ├── OrderMapper.java
│   ├── PointsMapper.java
│   ├── AddressMapper.java
│   ├── InviteCodeMapper.java
│   └── MessageMapper.java
├── service/
│   ├── AuthService.java
│   ├── AdminService.java
│   ├── OperatorService.java
│   └── UserService.java
├── controller/
│   ├── AuthController.java
│   ├── AdminController.java
│   ├── OperatorController.java
│   └── UserController.java

src/main/resources/
├── application.properties
└── mapper/                        # XML Mapper 文件

src/test/
├── java/com/sh/aishop/
│   ├── AuthControllerTest.java
│   ├── AdminControllerTest.java
│   ├── OperatorControllerTest.java
│   └── UserControllerTest.java
└── resources/
    └── application-test.properties # H2 测试配置

code/backend/sql/
└── init.sql                       # 数据库初始化脚本
```

---

## 五、实现顺序建议

```
第一步：基础设施（1.1 + 1.2 + 1.3）
    ↓
第二步：实体+枚举+工具（2.1 + 2.2 + 2.3 + 2.4）
    ↓
第三步：认证模块（3.1 + 3.2 + 3.3 + 3.4）
    ↓
第四步：管理员接口 + 测试（4.x + 7.1）
    ↓
第五步：店铺用户接口 + 测试（5.x + 7.2）
    ↓
第六步：普通用户接口 + 测试（6.x + 7.3）
    ↓
第七步：构建验证（mvn clean package）
```

---

## 六、待确认事项

| 问题 | 选项 | 推荐 |
|------|------|------|
| 数据库名 | `ai_shop` | ✅ |
| 后端端口 | `8080` | ✅ |
| JWT 有效期 | 7 天（可配置） | ✅ |
| 雪花 ID | 外部依赖（hutool IdUtil） | ✅ |
| 预置管理员 | `admin / admin123` | ✅ |
| Excel 导入 | 使用 EasyExcel | ✅ 轻量 |

---

**文档版本**：v1.0
**创建日期**：2026-05-09
