# 开发规范文档

## 一、数据库规范

| 规范 | 说明 |
|------|------|
| 不使用物理外键 | 通过逻辑关联维护数据一致性 |
| 不使用唯一键 | 逻辑删除后无法保证唯一性 |
| 枚举类型 | 用 `tinyint` + 备注替代枚举 |
| 删除方式 | 逻辑删除，所有表都有 `deleted` 字段 |
| ID 生成 | 雪花算法，转 String 传给前端 |
| 审计字段 | 所有表必须包含 `created_at`, `updated_at`, `created_by`, `updated_by`（纯关联表可酌情省略 `updated_at`） |

## 二、审计字段规范

### 2.1 字段约定

所有业务表必须包含以下 4 个审计字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `created_at` | DATETIME | 创建时间，`DEFAULT CURRENT_TIMESTAMP` |
| `updated_at` | DATETIME | 更新时间，`DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` |
| `created_by` | BIGINT | 创建人ID，指向 `user.id` |
| `updated_by` | BIGINT | 更新人ID，指向 `user.id` |

> 纯关联表（如多对多中间表）可酌情省略 `updated_at`、`created_by`、`updated_by`，但必须保留 `created_at`。

### 2.2 自动填充机制

项目通过 MyBatis-Plus 的 `MetaObjectHandler` 实现审计字段自动填充：

- `BaseEntity.java` 声明了 `createdAt`、`updatedAt`、`createdBy`、`updatedBy` 四个字段，并配置了 `@TableField(fill = ...)` 注解
- `MyMetaObjectHandler.java` 在 `insertFill` / `updateFill` 中自动写入当前时间和当前用户ID
- 当前用户ID 从请求属性 `userId` 获取（由 `AuthInterceptor` 拦截器解析 JWT 后注入）
- 非请求上下文（如注册、定时任务）时，`created_by` / `updated_by` 为 `NULL`

### 2.3 新建流程

1. 实体类继承 `BaseEntity<T>`
2. 在 `init.sql` 中添加对应的 4 个审计字段
3. 无需手动处理时间和用户，`MetaObjectHandler` 自动填充

## 三、枚举规范

### 3.1 核心原则

> **代码中严禁硬编码魔数/魔串，所有状态、类型、角色等固定取值必须使用枚举。**

### 3.2 后端枚举规范（Java）

所有枚举类需满足：
- 包含 `code`（int）和 `desc`（String）两个字段
- 提供 `getCode()`、`getDesc()` 方法
- 提供 `fromCode(int)` 静态工厂方法
- 枚举命名：名词性单数，首字母大写（如 `OrderStatus`、`RoleEnum`）

```java
// ✅ 正确：统一枚举模式
public enum OrderStatus {
    CREATED(1, "已下单"),
    CONFIRMED(2, "已确认"),
    SHIPPED(3, "已发货"),
    COMPLETED(4, "已完成"),
    CLOSED(5, "已关闭");

    private final int code;
    private final String desc;

    OrderStatus(int code, String desc) {
        this.code = code;
        this.desc = desc;
    }

    public int getCode() { return code; }
    public String getDesc() { return desc; }

    public static OrderStatus fromCode(int code) {
        for (OrderStatus e : values()) {
            if (e.code == code) return e;
        }
        return null;
    }
}
```

使用枚举比较，禁止直接比较数字：

```java
// ✅ 正确：使用枚举引用
if (product.getStatus() == ProductStatus.ON_SALE.getCode()) { ... }

// ❌ 错误：硬编码魔数
if (product.getStatus() == 1) { ... }
```

错误码使用 `ResultCode` 常量类，同样禁止硬编码：

```java
// ✅ 正确
return Result.fail(ResultCode.PRODUCT_NOT_FOUND, "商品不存在");

// ❌ 错误
return Result.fail(2003, "商品不存在");
```

### 3.3 前端枚举规范（TypeScript）

前端使用 `as const` 对象模拟枚举，配合类型推导：

```typescript
// ✅ 正确：统一枚举模式
export const OrderStatus = {
  PENDING: 1,    // 已下单
  CONFIRMED: 2,  // 已确认
  SHIPPED: 3,    // 已发货
  COMPLETED: 4,  // 已完成
  CLOSED: 5,     // 已关闭
} as const

export type OrderStatusValue = typeof OrderStatus[keyof typeof OrderStatus]

// 配套展示映射
export const OrderStatusText: Record<number, string> = {
  [OrderStatus.PENDING]: '已下单',
  [OrderStatus.CONFIRMED]: '已确认',
  [OrderStatus.SHIPPED]: '已发货',
  [OrderStatus.COMPLETED]: '已完成',
  [OrderStatus.CLOSED]: '已关闭',
}
```

使用枚举引用，禁止直接比较数字：

```typescript
// ✅ 正确：使用枚举引用
if (order.status === OrderStatus.PENDING) { ... }

// ❌ 错误：硬编码魔数
if (order.status === 1) { ... }
```

> 前端枚举定义统一放在 `src/types/enums.ts`，按功能区域分块注释。

## 四、错误处理规范

### 4.1 统一响应结构

所有 API 响应统一使用 `Result<T>` 包装：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

### 4.2 错误码规范

| 区间 | 类别 | 说明 |
|------|------|------|
| 200 | 成功 | - |
| 400 | HTTP 错误 | 仅通用失败 |
| 401 | 未认证 | Token 失效 |
| 403 | 无权限 | 无访问权限 |
| 500 | 服务器错误 | 兜底 |
| 1000-1099 | 认证模块 | 登录、注册、Token |
| 1100-1199 | 验证异常 | 参数校验、格式错误 |
| 2000-2999 | 业务错误 | 商品、订单、积分等 |

### 4.3 全局异常处理

项目使用 `@RestControllerAdvice` 的 `GlobalExceptionHandler` 统一处理异常。

### 4.4 Service 层错误返回

```java
// ✅ 正确：Service 层直接返回 Result
return Result.fail(ResultCode.PRODUCT_NOT_FOUND, "商品不存在");

// ✅ 正确：需要抛出业务异常时
throw new BusinessException(ResultCode.INVITE_CODE_INVALID, "邀请码无效");

// ❌ 错误：不要在 Service 层抛异常让 GlobalExceptionHandler 捕获
```

## 五、用户角色

| 角色 | 值 | 说明 |
|------|---|------|
| 管理员 | 1 | 系统唯一，管理所有店铺和用户 |
| 店铺用户 | 2 | 运营自己店铺，发行积分 |
| 普通用户 | 3 | 属于某店铺，兑换商品 |

> 所有角色判断必须使用 `RoleEnum` 枚举，禁止直接比较数字。

## 六、订单状态流转

```
已下单 (1) → 已确认 (2) → 已发货 (3) → 已完成 (4)
     ↓           ↓
   关闭(5)     关闭(5)
```

> 所有状态判断必须使用 `OrderStatus` 枚举，禁止直接比较数字。

## 七、ID 生成规范

- 使用雪花算法生成分布式 ID
- 后端存储为 `BIGINT`（Long）
- 传给前端时转为 String，避免 JS 精度丢失
- 枚举值、错误码等小型整数不使用雪花 ID

## 八、注意事项

1. **雪花ID精度**：返回 Long 给前端时需转为 String，避免精度丢失
2. **逻辑删除**：查询时注意 `deleted = 0` 条件
3. **店铺营业状态**：歇业时普通用户可浏览但无法下单
4. **邀请码**：管理员/店铺用户可生成，普通用户注册需审核
5. **枚举先行**：新增状态/类型时，必须先定义枚举，再使用枚举值
